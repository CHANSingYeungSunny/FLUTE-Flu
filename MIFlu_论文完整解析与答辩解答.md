# MIFlu 论文解析与答辩解答（导师问题逐条版）

> 目的：只保留导师实际提出的若干问题，逐条讲清、讲透。所有口径以 `MIFlu_paper.md`（论文 PDF 的直接完整 markdown 转换，IEEE JBHI 2025）为唯一权威交叉参考源；旧 `MIFlu_Complete_Extraction.md` 已删除，不再引用。
> 复现现状（已核实）：当前训练/评估已严格采用论文协议 `L∈{24,36,48,60}` 四档独立、70:10:20 静态切分、scaler 仅 fit 于训练集；L=52 walk-forward 已废弃隔离（仅作 Phase-2 ChatTime 对照，非本次复现主路径）。当前 L=24 National（train-only 空间，10-rep 均值）MSE=5.056、MAE=1.500，论文 Table V 为 MSE=1.542、MAE=0.726（分别 3.28×、2.07×）。逐位复现未达到（论文未公开 lr/epoch、代码与数据快照），但协议已对齐，残余偏差来源见第 7 节。

---

## 第 0 部分：一句话讲清 MIFlu 在做什么

MIFlu 要解决的问题：**给定过去 104 周（≈2 年）的 7 项流感监测数据，预测未来 L 周（L∈{24,36,48,60}）的全国 ILITOTAL（流感样疾病就诊总人次）**。

创新点不在"预测"本身，而在于**把一段英文领域知识描述（Text Prompt）和数值时间序列拼进同一个 GPT2，让语言模型同时"读文字"和"读数字"再输出预测**——标题里 "Multimodal" 指"自然语言领域知识 + 数值时间序列"这种多模态，而非"图像+文字"。

---

## 第 1 部分：Input / Process / Output（把对方当完全不懂这行的人讲）

### 输入（Input）—— 两条完全不同的轨道

**轨道 A：数字轨道（Time-Series）**——7 个变量，每个是过去 104 周的一串数字：
1. `% WEIGHTED ILI` — 加权流感样疾病就诊比例
2. `% UNWEIGHTED ILI` — 未加权流感样疾病就诊比例
3. `AGE 0-4` — 0–4 岁患者数
4. `AGE 5-24` — 5–24 岁患者数
5. `ILITOTAL` — **要预测的目标本身**（历史值也作为输入的一部分，因为预测下一步要看过去的自己）
6. `NUM. OF PROVIDERS` — 参与上报的医疗机构数量
7. `OT` — 论文未公开定义的辅助通道（我们的实现里用就诊总量全量未缩放，即 `num_patients`；论文 Table 仅称其为 "'OT' feature for long-term forecasting"，未给公式）

> **为什么选 `num_patients` 作为 OT（简要推导）**：论文 Table X 只写 "'OT' feature for long-term forecasting task"，**未给公式、未称黑箱**（旧提取文件曾误称 "black box best guess"，已废弃）。我们据此反推实现：核对原始下载数据，OT 列最大值约 **224 万**，与论文 Figure 2(a) 中 Variable 7 的约 **180 万**量级吻合 → 说明 OT 是"全国总就诊人次/分母"量级的宏观计数，而非百分比或缩放值。故实现为 `num_patients`（CDC 总就诊人次/分母，全量未缩放）——这是**我们自己的实现选择，非论文定义**，并在 `textual_embedder.py` 中标注。此前文档曾误写为"除以 100"，已修正；训练代码本身从未做过该缩放，StandardScaler 对常数缩放不变，因此量级差异不影响已训练模型的正确性。论文原文仅称 'OT feature for long-term forecasting task'，未给出明确计算公式；OT=num_patients（全量）是基于 Fig 2(a) 量级反推出的强证据支持的实现选择。

> 为什么是这 7 个**异质**通道？它们覆盖"结果量（ILITOTAL）""总体流行强度（WEIGHTED/UNWEIGHTED ILI）""人群分布（AGE 0-4 / 5-24）""监测口径（NUM. OF PROVIDERS）""辅助上下文（OT）"五个侧面。异质是指它们的量纲与物理含义不同（比例 vs 计数 vs 机构数），但都被各自归一化后拼成 `(N=7, T=104)` 的输入矩阵，模型用全部 7 个变量的历史模式共同预测 ILITOTAL 这一维。论文 Table II 严格就是这 7 个，N 必须等于 7，不要自造"AGE 25+"去凑。

**轨道 B：文字轨道（Text Prompt）**——固定模板英文描述，分三段：
1. **数据集说明**：数据来源、频率（周）、覆盖范围。
2. **变量描述**：7 个变量的统计特征（均值/趋势/季节性）+ 领域知识（如"流感通常每年冬季有一个高峰"）。
3. **任务指令**：明确写 "Predict the next L steps given the previous T steps"。

> **关于 step size 的关键澄清（导师指定）**：复现时必须用 **L=24** 作为其中一档（论文四档为 L∈{24,36,48,60}），与原文一致。为什么"104 周预测 24 周"？因为（a）流感季节性跨自然年，看 2 年（104 周）才学得清"正常季节波动 vs 特殊年份异常"；（b）业务上疫苗运输/分发、接种计划、政策制定需提前约 6 个月（≈24 周）准备期，故国家级做 L≥24 的长期预测。Prompt 末行即写死 "Predict the next 24 steps given the previous 104 steps"。**我们完全遵循 MIFlu 论文方法，不是 ChatTime 的 hist=104/pred=52/stride=52。**

### 处理（Process）—— 两条轨道怎么变成"同一种语言"

- **GPT2 只能读固定维度的向量**，既不能直接读英文单词，也不能直接读原始数字，所以两条轨道都要先"翻译"成向量：
  - 文字轨道：用 GPT2 自带 **Tokenizer** 把句子切成 token，每个 token 查表得 768 维向量（标准 NLP）。
  - 数字轨道：用 **Patching**——把连续一段数字（patch 长度 Lp=24、步长 S=2）先归一化，再用**可训练的线性层**投影成同样的 768 维向量。这个线性层就是"数字世界的分词器"。
- 两条轨道投影到**同一 768 维空间**后，在序列维度首尾拼接：`h_fuse = [h_text ‖ h_time]`。
- 拼接向量送进 **GPT2 的前 6 层**（K=6，不是全部 12 层），只对这 6 层的注意力模块做 **LoRA 微调**（r=4，只训练新增少量参数，原始 GPT2 权重基本冻结；位置编码与 LayerNorm 冻结）。
- GPT2 是 **Decoder-only（自回归，只能看过去）**，与"用过去预测未来"本质吻合，这是选 GPT2 而非 BERT/Word2Vec 的核心原因。

### 输出（Output）

- GPT2 输出向量经**线性投影层**变回数字，得到未来 L 步的**归一化预测值**。
- 反归一化还原真实患者数量级，并 `clamp ≥ 0`（患者数不可能为负）。
- 只取 **ILITOTAL** 这一维作为最终输出（模型内部用了 7 个变量信息）。
- 输出投影实现：丢弃文本 token、保留时间 patch 部分，对 patch 维做**均值池化**后投影到 `N×L`（`scripts/miflu_model.py` L334-336）。**此均值池化是对 GPT4TS 的标准复刻，不是破坏时间结构的 bug。**

**一句话总结**：7 个变量一起喂进模型，模型用全部 7 个变量的历史模式 + 文字领域知识，预测其中 1 个变量（ILITOTAL）的未来走势。

---

## 第 2 部分：具体例子——预测 2019-07-01 那一点用了什么输入

导师要你能具体回答"预测某一天，输入到底是什么"。用固定窗口（非 walk-forward）最直观：

假设要预测的窗口起点是 **2019-07-01**（CDC 周索引约为 epiweek **201112**，覆盖 2019-06-25–2019-07-01 这一周）：

- **数字输入**：取 **epiweek 201112 往前 104 周**，即 **epiweek 200913 至 201111**（约 **2017-07-04 至 2019-06-24**）的 7 个变量真实历史数值。**只能使用 201112 之前已经发生的数据，绝对不能用 201112 及之后的任何数据**（未来信息泄漏红线）。
- **文字输入**：同一段固定模板 Prompt，其中"变量描述"的统计数字（均值/趋势/季节性）也**只能用 epiweek 200913–201111 这 104 周历史窗口内的统计量**计算，不能用测试集或未来数据算出的统计量。
- **模型输出**：从 epiweek **201112** 起未来 L=24 步的 ILITOTAL 预测序列，即 epiweek **201112 至 201135**（约 **2019-06-25 至 2019-12-17**）共 24 个周点的预测值。

> 注：CDC 数据用 **epiweek**（如 200913 = 2009 年第 13 周）而非 calendar date；"2019-07-01"是按周对齐后的近似举例。严格应说"预测目标是 epiweek 201112 起未来 24 周"。

即 MIFlu 是**用固定长度历史窗口（T=104）一次性预测未来一整段（L 步）**，不是逐周滚动预测单点——这是"long-term forecasting"定义，而非"one-step-ahead"。

---

## 第 3 部分：为什么 70:10:20 切分（让顶刊审稿人信服的解释）

**论文依据**：Section V-A 明确写 "Following previous studies [9], [21], we split the data into training, validation, and test set at a ratio of 70:10:20"。

可站得住脚的解释角度：
1. **这是回归任务**（`Y = f(X)`，`X∈R^(N×T)`，Y 连续数值），无类别标签，切分逻辑可纯粹按时间比例。
2. **时序必须按时间顺序切分，shuffle=False**。随机打乱会让未来点进训练集去预测过去点——最常见最致命的 look-ahead bias。70:10:20 = 前 70% 训练、中 10% 验证（早停/调超参）、后 20% 测试。
3. **比例本身沿用人前工作（[9] GPT4TS 等）标准做法**，目的是和同数据集上的 PatchTST/GPT4TS 等基线**可比**——切分比例不同，MSE/MAE 根本无法比较，这是对照实验基本要求。
4. **具体数字**：全国数据约 1002–1040 周，70:10:20 约 Train≈717、Val≈102、Test≈206。脚本输出 717/102/206 是因要预留构造"目标偏移"所需尾部样本（预测 L=60 时最后 60 点无法构成完整"输入+标签"对，标准时序切分都会扣掉），属常规操作非数据错误。

---

## 第 4 部分：NUM. OF PROVIDERS 是主因还是倒果为因

这是你之前表述最有问题的地方，必须诚实准确回答，而**不是简单复述"贡献度 34.7%"**。结论先给：

> **一句话结论**：NUM. OF PROVIDERS 既不是"纯倒果为因"，也不是"流行病学主因"。ILITOTAL 的**定义本身就是**参与上报的 providers 汇总的病例计数，所以二者每年冬季必然同涨同落——这是**监测口径 / 采样框架效应（sampling-frame / denominator effect）**，不是"provider 数量导致流感传播"。但 Granger 检验显示 PROVIDERS 在时间上**单向领先** ILITOTAL，说明它确实携带一点真实领先信息，不是纯噪声。因此它是"真实领先信息 + 采样框架效应"的混合。

### 主证图：GT ILITOTAL vs NUM. OF PROVIDERS 同轴叠加（直接回答"主因还是倒果为因"）

文件：`results/ili/figures/q4_overlay_ilitotal_vs_providers.png`

- 同一周轴上叠加两条**真实（ground-truth）**曲线：ILITOTAL（蓝，左轴，万级计数）与 NUM. OF PROVIDERS（橙，右轴，千级计数），覆盖全序列 2002–2021（1025 周），黄色阴影标出流感季（约 10 月–次年 5 月）。

![](results/ili/figures/q4_overlay_ilitotal_vs_providers.png)
- **读图（这张图直接回答问题）**：两条线**每年冬季同步出现峰**——流感季一来，上报机构数略升、被计入的病例汇总计数 ILITOTAL 大升，二者形状高度吻合。这本身就说明 PROVIDERS **不是"倒果为因"那种纯噪声**：如果它只是"病例多了才多报"的滞后结果，它应该跟在 ILITOTAL 后面、而非每年稳定同步甚至略领先地起峰。
- **为什么共动不是因果（监测口径效应，机械部分）**：这种"每年冬季必然同涨同落"，根本原因是 **ILITOTAL 的定义就是"所有上报机构报告的 ILI 病例总数"**——PROVIDERS（上报机构数）本身就**长在 ILITOTAL 的定义里**，好比"全校学生总数"和"教室数"必然一起变。多一家诊所，总数自动变大，不是因为流感变多，而是网撒大了。所以这是**会计核算关系，不是因果**，PROVIDERS 也**不是流行病学意义上的"主因"**（provider 数量不会"导致"流感传播）。**一句话：主图已给出答案——PROVIDERS 既非纯倒果、也非流行病学主因，而是"真实领先信息 + 监测口径效应"的混合。** 下面两张附录图进一步解释并支撑这一结论。

### 附录（支撑/解释主图结论：为什么是混合而非纯因果或纯倒果）

**图 A：双向 Granger 因果图** — 文件：`results/ili/figures/q4_granger_bidirectional.png`

![](results/ili/figures/q4_granger_bidirectional.png)

箭头 X→ILITOTAL 检验"X 是否统计领先 ILITOTAL"，箭头 ILITOTAL→X 检验反向。关键结果（train-only，MAX_LAG=24）：

| 变量 | p（X→ILITOTAL） | p（ILITOTAL→X） | 解释 |
|---|---|---|---|
| NUM. OF PROVIDERS | **1.2×10⁻⁶** ✅ | **0.11** ❌ | **单向领先 ILITOTAL**，不是纯倒果为因 |
| % WEIGHTED ILI | 3.1×10⁻⁴ | 3.7×10⁻³¹ | 双向显著，共被流感季节驱动 |
| % UNWEIGHTED ILI | 6.7×10⁻⁴ | 2.9×10⁻³⁸ | 双向显著 |
| AGE 0-4 | 1.0×10⁻⁵ | 3.6×10⁻²³ | 双向显著 |
| AGE 5-24 | 5.5×10⁻⁶ | 1.2×10⁻⁹ | 双向显著 |
| OT | 3.3×10⁻¹² | 2.2×10⁻⁴ | 双向显著 |

只有 PROVIDERS 对 ILITOTAL 是**单向显著领先**（反向不显著）→ 它确是统计领先指标，不是"病例多了才多报"那种纯倒果为因。

- **真实领先信息（真实预测部分）**：Granger 检验显示 PROVIDERS 在时间上**单向领先** ILITOTAL（p=1.2×10⁻⁶，反向不显著）。白话讲：机构数的变化往往**略早于**病例数的变化，不是滞后跟着跑，说明它带了一点真实的预报信号，不是纯噪声、也不是纯滞后副产品。
- **混合 = 两层叠加**：PROVIDERS 对预测有用，但"为什么有用"有两部分——一层是它本就是目标的定义组成部分（口径效应，机械），一层是它确实在时间上领先（真实领先信息）。所以模型/RFe 给它高重要性是这两者叠加，**绝不能说"机构数导致流感"**。
- **对导师一句话**："PROVIDERS 能帮预测 ILITOTAL 有两个原因：一是机械的——ILITOTAL 本来就是从这些机构汇总出来的，所以按定义就一起动；一是真实的——机构活动在时间上略领先病例。它是'定义性核算效应 + 一点真实领先'的混合，不是'诊所越多流感越多'。"

**图 B：ILITOTAL vs NUM. OF PROVIDERS 散点 + OLS** — 文件：`results/ili/figures/q4_ilittotal_vs_providers_scatter.png`

![](results/ili/figures/q4_ilittotal_vs_providers_scatter.png)

- **R² = 0.496，Pearson r = 0.704**；OLS 斜率 ≈ 12.35（每多一个 reporting provider，ILITOTAL 机械多约 12 例）。
- 这印证二者是**强机械/采样规模相关**：上报机构越多，可被计入的患者基数越大，ILITOTAL 作为汇总计数自然偏大（即图 A 所说的监测口径效应在截面上的体现）。

### 综合结论（建议原话，写给导师/审稿人）

> "RF/SHAP 显示 NUM. OF PROVIDERS 统计解释力占比最高（34.7%），但叠加时序图 + 双向 Granger + 散点回归揭示其重要性是**真实领先信息与监测口径效应的混合**：ILITOTAL 本质就是 providers 上报病例的汇总计数，故二者每年冬季必然共动（采样框架效应，非因果）；但 PROVIDERS 对 ILITOTAL 有显著单向领先（Granger p=1.2×10⁻⁶，反向不显著），说明它携带真实领先信息。我们不把它称为流行病学因果主因，而在 Limitation 中明确这是统计关联与采样框架效应的混合，并配图证明。"

**顶刊为什么认这套**：流行病学/方法学期刊（如 CDC FluSight 方法论文、Lancet/Nature 子刊的监测研究）普遍用"分母效应 / 监测口径"解释 ILINet 各指标的共动，并用 Granger 先导性区分"真实信号"与"机械共动"。主动说清"统计显著 ≠ 因果"并用图证明，恰恰是加分项，不是漏洞。

> 备注：主证叠加图由 `scripts/make_q4_overlay_figure.py` 生成；Granger 图与散点图由 `scripts/make_q4_causality_figures.py` 生成；原始 p 值见 `data/q4_causality_summary.csv`。

---

## 第 5 部分：National 预测偏差溯源（MSE/MAE 残酷审计）

导师要求分析"为何之前 MSE/MAE 与原文差那么多"，并下次复现至少接近原文。

**已核实的当前状态（代码与数据说话）**：
- 协议已对齐论文：`L∈{24,36,48,60}` 四档独立、70:10:20 静态切分、scaler 仅 fit 训练集（无泄漏）。
- 当前 L=24 National（train-only 空间，10-rep 均值）：**MSE=5.056 ± 1.583，MAE=1.500 ± 0.121**。
- 论文 Table V（L=24 National）：**MSE=1.542，MAE=0.726**。
- 即当前偏离 **MSE 3.28×、MAE 2.07×**（注意：这是修正后的真实偏差，不是早期非法负值 bug 时代的错误数字）。

> 诊断图（L=24，train-only 空间，10-rep 均值）：各通道/各误差项分解见 `results/ili/figures/fig_miflu_eval_diagnostics_L24.png`。

![](results/ili/figures/fig_miflu_eval_diagnostics_L24.png)

**偏差分层排查（诚实版）**：
1. **早期偏差主因是协议错误**：之前误用 L=52 walk-forward（ChatTime 风格），与论文 L∈{24,36,48,60} 静态切分定义 train/test 不同，数字天然不可比。此已修正。
2. **已排除的原因**：Scaler 泄漏（scaler 仅 fit 训练集，已出泄漏审计）、其他 6 通道拉高平均误差。**部分处理／未完全关闭**：数据版本差异——"2024→2026 CDC 回溯修订"这一具体假设已证伪（本地数据无 2024+ 周，双方均覆盖 2002–2021），但 CDC 在 2002–2021 *区间内*的常规回溯修订（ILINet 周度数据会定期被重新加权/修订）仍是**未排除候选**（Option 3）。
3. **关于"预测头 patch 池化破坏时间结构"的假设——已撤回**：实读 `scripts/miflu_model.py` 确认输出投影的 patch 均值池化是对 GPT4TS 的标准复刻，不是 bug，不能作为偏差根因。
4. **残余偏差的未确认候选**：
   - **Scaler 拟合范围**：论文未说明是 train-only 还是 pooled。将当前 train-only 预测**代数重缩放**到假设的 pooled 空间（线性映射，未重训），得 MSE≈1.61（距论文 1.542 约 4%）、MAE≈0.85（距论文 0.726 约 17%）。**线性缩放对各误差点同一比例，不存在"压缩大误差更狠"**；MSE 接近但 MAE 仍差 17%，故 pooled-fit 只是**未确认的部分解释**，非确认根因。
   - **CDC 区间内回溯修订**：仅排除 2024+ 增量周，未排除 2002–2021 span 内 CDC 小幅度回溯修订，列为未排除候选（Option 3）。
   - **论文未公开实现细节**：lr/epoch 未公开（只写 "set empirically"），无官方代码/数据快照；RevIN 具体配置、文本融合细节未知。这些无法在不拿到代码/数据下解决。
5. **预期管理**：逐位数字完全对齐不现实，合理复现目标是"同一量级、同一趋势、同一结论"（误差量级 1–2 倍内、峰值捕捉能力接近）。这需提前与导师对齐，否则会陷入"永远复现不出小数点"的死循环。

---

## 第 6 部分：图里是否混用了训练/测试数据（导师怀疑左准右差）

**直接回答：不是混用了训练/测试数据。**

（注：早期曾误用 ChatTime 的 walk-forward 协议 hist=104/pred=52/stride=52，会人为制造左准右差假象，现已废弃改回论文协议；但下面要说明的是——**即使换成正确论文协议，左准右差依然存在**，所以这不是协议错配问题。）

**为什么在现在的论文协议（L∈{24,36,48,60}、70:10:20 静态切分、画图严格按 `split=='test'` 过滤、scaler 仅 fit 训练集）下，左准右差仍然出现？** 两个合法原因，都**不是数据混用**：

1. **时间距离 / 分布漂移（主因）**：静态时间切分下，训练集是前 70%（到 717 周），测试集是最后 206 周（≈2020–2021）。test 曲线按时间从左到右排列，于是**左边 = 离训练窗口近的 test 周（≈2019–2020）**，模型学到的季节模式仍匹配 → 拟合好；**右边 = 离训练窗口远的 test 周（2021）**，漂移累积 → 偏差大。所以"左好右差"本质就是"离训练近的好、离训练远的差"，是任何静态时间切分预测都预期会有的现象，与是否混用数据无关。
2. **新冠结构性断裂正好压在右半段**：test 期 2020–2021 覆盖疫情，口罩/隔离等非药物干预使流感发病率断崖式跌到历史罕见的低平基线，模型在 2002–2019 训练数据里从没见过。模型按"每年冬季必有峰"的先验，在右半段**凭经验编造了两个不存在的假峰**，而真值是平的 → 右侧剧烈发散。这是**异常体制下的外推失败（extrapolation failure）**，不是泄漏。

**到底有没有混用训练/测试数据？没有。** 可以自证：
- 画图只取 `split=='test'` 的行，训练/验证点根本没进这条曲线；
- scaler / RevIN 的 `fit` 只在训练集做，`transform` 才应用到 test；
- test 每个点的目标值，模型在训练时从未见过（test 索引严格 > train+val 终点）。
左准右差纯粹是"时间漂移 + 新冠异常期"导致的**真实泛化局限**，可坦然向导师说明，并附泄漏自查（split 列断言 + scaler fit 边界单测）自证清白。

> 评估连续图（L=24，test-only，GT 虚线 vs 预测实线）：`results/ili/figures/fig_miflu_eval_continuous_L24.png`。左半（近训练窗口）贴合、右半（新冠异常期）发散，即文中"左准右差"。

![](results/ili/figures/fig_miflu_eval_continuous_L24.png)

---

## 第 7 部分：Timing (|Δt|) 现在怎么算、问题解决了没

**直接回答：问题已解决。** Timing 现在按 MIFlu 论文协议正确计算，不会再出现 0.0 的假象。下面讲清楚"现在 Timing 到底是什么、怎么算、为什么现在是可信的"。

**1. Timing (|Δt|) 的物理定义（现在）**
Timing = **预测峰值相对真实峰值的时间偏移（单位：周）的绝对值均值**。它衡量"峰什么时候到"预测得准不准——比如真实峰值在第 10 周、模型预测峰值在第 12 周，则这一峰的 Δt = |12−10| = 2 周。Timing 越小越好（论文阈值 ≤ 2.0 周）。

**2. 现在怎么算（已修正的逻辑，`compute_miflu_indicators.py`）**
- 峰值检测：对 test-only 的真实曲线和预测曲线，**用同一套** `find_peaks` 参数（prominence、distance）各自找峰，保证公平。
- 匹配：每个真实峰在预测峰里找时间在 ±2 周容差内、且最近的那个；若找到 → 标记 `Hit`，记 `delta_t = 预测峰位置 − 真实峰位置`（周）；若找不到 → 标记 `Missed`（漏检），**绝不算成 0**。
- Timing 只对所有 `Hit` 峰的 `|delta_t|` 求平均；**同时强制报告漏检峰数**（如 "1/4 peaks matched"），所以 Timing 永远不会脱离 Peak Hit 上下文单独出现。
- 若 0 个峰命中，Timing 报 `null`（不是 0.0）。

**3. 为什么之前的 0.0 不见了、现在是可信的**
之前 0.0 是因为误用 ChatTime walk-forward 协议使峰值时间对齐错位，恰好只命中 1/4 峰且那一个峰 Δt 恰为 0，均值就被拉成 0.0，掩盖了另 3 个漏检峰。现在切回论文协议（L∈{24,36,48,60} 静态切分、test-only）+ 上述修正逻辑后：
- 峰值在正确的 test 窗口上检测，时间对齐正确；
- 漏检峰明确记为 `Missed` 而非 0，Timing 只统计命中的峰且强制带漏检数；
- 当前 L=24 实测 Timing ≈ 0.5 周（Hit 2/4），如实反映了"模型在正常季节性年份峰时间抓得准、但在新冠异常期漏检/错峰"的真实情况。

**4. 物理意义（导师要的"不只是数学"）**
Timing 不是孤立数字：它和 Peak Hit 必须一起看。比如现在 Peak Hit 只命中 2/4 峰，说明模型在 2020 年中后的新冠异常期**凭季节性先验编造了两个假峰**（真值低平），这两个峰既没命中真值峰、又抬高了误差——这是**外推失败（extrapolation failure）**，不是指标 bug。正常季节性年份（2018–2020 初）模型峰时间抓得准（Timing 小）。这既是诚实局限性，也是后续"引入异常检测/更多领域知识"创新的动机。

**给导师的现态表述**：
> "Timing (|Δt|) 现在按论文协议正确计算：对 test-only 曲线用同一套峰值检测，匹配 ±2 周容差内的预测峰，Timing 只统计命中峰的 |时间偏移| 均值，并强制同时报告漏检峰数（如 2/4 matched），0 命中则报 null 而非 0。之前的 0.0 是误用 ChatTime 协议导致的口径错乱，现已修复；当前 L=24 Timing ≈ 0.5 周，如实反映模型在新冠异常期漏检/错峰的外推局限。"

> 峰值/ Timing 的视觉效果见第 6 部分嵌入的连续评估图 `fig_miflu_eval_continuous_L24.png`（GT 虚线 vs 预测实线，峰的命中/错峰一目了然）。
