# 2026-08-14 — MIFlu 重构为 ChatTime 协议（N=7 恢复 + walk-forward）

## 本轮变更（Mission 2：逆转上一轮 OT 清理）
- 用户逆转了「purge OT」决策，改为**恢复 N=7 架构**，OT 作为第 7 通道 = `num_patients`（总门诊量/分母），如实标注为 **black-box best guess**（论文 OT 含义不可核对，量级差 3 个数量级；StandardScaler 对常数缩放不变故不影响训练尺度）。
- 文件夹布局镜像 ChatTime：`scripts/`、`data/raw/`、`data/ili/`、`results/ili/{figures,metrics,pc_results}/`、`docs/`。
- 删除旧 per-horizon 出图管线：`make_forecast_figure.py`、`figure_q1*.sh`、`train_q1_L{24,36,48,60}.sh`、`train_q1_sweep.sh`、`debug_L60.sh`、`hpc_results/*`。旧 `best_miflu_L{24,36,48,60}.pth` 归档至 `checkpoints/`。

## 关键文件状态
- `train_miflu.py`：N=7，`--horizon 52`（单 horizon），日志/结果→`data/`，checkpoint→`checkpoints/best_miflu_L52.pth`。
- `textual_embedder.py`：`build_prompt`/`VAR_COLS` 恢复为 7-var primary，`PROMPT_TEMPLATE_7` 含 OT doc 行；`PROMPT_TEMPLATE_6` 降为 fallback。
- `generate_walkforward.py`：单 L=52 非重叠 walk-forward（hist=104/pred=52/stride=52，70:10:20，末窗口 `min(pred,remain)` 截断），输出 `results/ili/miflu_fulltest_walkforward.csv`。
- `compute_miflu_indicators.py` / `make_miflu_evaluation_figure.py`：默认输入指向 `miflu_fulltest_walkforward.csv`，输出 `miflu_` 前缀（2 图 + verdict md + 指标 csv/json）。4 指标阈值对齐 ChatTime（Peak Hit≥0.75 / Timing≤2.0 / Peak Intensity≤20.0 / Direction≥0.60）。
- `docs/ILI_FOUR_INDICATORS.md`、`docs/FIGURE_STYLE_GUIDE.md`：MIFlu 版（镜像 ChatTime）。
- `MIFlu 复现障碍分析.md`：Q1 改为 ChatTime 协议管线 + 新增 Q1.5（为何 hist=104/pred=52/stride=52 vs 论文 Pred=L）；Q6 第 7 通道 OT 标注 black-box best guess。

## HPC 命令块（待用户执行，本机无法 SSH）
- scp 完整脚本+data/raw 到 `burgundy.hpc.cityu.edu.hk:/home/sychan552/scratch/MLFLU/`（exclude .git/__pycache__/*.pyc）。
- 顺序提交（避免并发 NaN）：`sbatch scripts/train_q1_L52.sh` → 完成后 `sbatch scripts/infer_walkforward_L52.sh`。
- 拉回 `results/ili/*` 后本机 CPU 跑 `compute_miflu_indicators.py` + `make_miflu_evaluation_figure.py`。
- scratch 路径用 `/home/sychan552/scratch/MLFLU/`（新，区别于旧 MLFlu_Q1）。
- 全部脚本已 `py_compile` 通过。

## 待回填
- HPC 实跑后把 `miflu_fulltest_walkforward.csv` 的真实峰值/指标填入 `MIFlu 复现障碍分析.md` Q1 §3.1。

## L52 训练提交（当晚第二轮）
- 提交 `sbatch scripts/train_q1_L52.sh` → job **504260** 秒退（file not found）：脚本原 `cd $HOME/scratch/MLFLU` 但 `train_miflu.py` 实际在 `scripts/` 子目录。
  - 修复：本地改脚本第31行 `cd $HOME/scratch/MLFLU/scripts`，重传 `.sh`，`scancel 504260` 后重提 → job **504263**（stingy/a100-04）正常启动，已进入 `[L=52] Rep 1/10` 训练循环。
  - 注意：`train_miflu.py` 内部用 `PROJECT_ROOT=dirname(dirname(__file__))` 定位 data/checkpoints，故 cd 到 scripts/ 不影响数据/输出路径。
- 路径真相：`$HOME` 在 HPC 实际 = `/gpfs1/scratch/sychan552`，`/home/sychan552` 与 `/gpfs1/scratch/sychan552` 是同一目录的两条路径。
- 用户偏好（已确认）：① 旧 `best_miflu_L{24,36,48,60}.pth`（本地+HPC）**保留**不删，留作与 MLFlu 论文对比；② 未来 scp 大文件时每几秒汇报进度（用 `scp -v` + 轮询远程文件大小），勿干等 echo；③ A100/stingy 节点配置保持不动。

## 文档清理（当晚第三轮）
- **删除的过时/错误交叉参考文件**（用户确认）：`reference_papers/MIFlu_extracted.txt`、`reference_papers/MIFlu_layout.txt`、`reference_papers/page_table2.txt`（错误提取）、`miflu_ground_truth.json`（错误真值基准）、`REPRODUCTION_PROOF_V2_ZH.md`（旧复现证明，被新协议推翻）、`MLFlu_forecast_figure.md`（基于退役脚本）。
- **保留**：全部 6 个 PDF 论文原文 + `MIFlu_Complete_Extraction.md`（作为**唯一交叉参考**）。
- **约定**：今后核对论文口径只用 `MIFlu_Complete_Extraction.md`，不再引用任何提取 .txt / json / 旧 md，避免错误数据污染结论。
- `MIFlu 复现障碍分析.md` 已整篇精简为大白话：Q1（2 图 + 4 指标 verdict）、Q2（L=24 理由 + 改 L=52 因 follow 医学期刊 golden standard）、附 L=104 四点理由、Q5（旧 L 表标注"已被取代"，新 L52 表待回填）、Q6（7 变量 + input/process/output 一句 + OT=num_patients/100 + 7 指标融合公式）。
- 自动化 `mlflu-l52` 已删除；2 分钟轮询改为我手动每 2 分钟只读 ssh 查 job 504263 直至 `best_miflu_L52.pth` 生成。
- **L52 训练完成**（job 504263，21:13 结束，28.1min，test_mse_ili=6.473）：`checkpoints/best_miflu_L52.pth`(329MB) + `data/results_miflu_20260814_204437.csv` 已 scp 拉回本机。
- **文档精简补刀**：按用户要求删除 Q4 段落（No. of Providers 因果分析），Q3/Q5 直接相接；Q3 正文去掉 Q4 引用。
- **推理已提交**：`sbatch scripts/infer_walkforward_L52.sh` → job **504361**（stingy 排队中），产出 `results/ili/miflu_fulltest_walkforward.csv` 后拉回本机，CPU 跑 `compute_miflu_indicators.py` + `make_miflu_evaluation_figure.py` 回填 Q1/Q5 的 4 指标与 2 图。

## 推理 bug 修复链（重要）
- **504361**：脚本 `cd $HOME/scratch/MLFLU` 但 `generate_walkforward.py` 在 `scripts/`，python 找不到脚本 → 静默退出。修复：cd 改 `scripts/`。
- **504363**：`--ckpt checkpoints/best_miflu_L52.pth` 相对 cwd(scripts/) 找不到 → `[BLOCKER] checkpoint not found`。修复：改用绝对路径 `/home/sychan552/scratch/MLFLU/checkpoints/best_miflu_L52.pth`。
- **504365**：模型加载成功但 `generate_walkforward.py:140` 日期切片 `df["epiweek"].iloc[start+T: start+T+length]` 越界（`IndexError: index 49`）。根因：末窗口 `length=L_total-i`，切片 `start+T+length` 超出 df 行数，pandas 静默截断导致 `wk` 比 `length` 短。修复：改为 `df["epiweek"].iloc[start: start+length]`（truth 周对应索引 i..i+length-1）。
- **504367**：✅ 成功，产出 `results/ili/miflu_fulltest_walkforward.csv`（205 周，test_start=820，stride=52，pred=52）。脚本已改并留档本机 `scripts/`。

## 最终 L52 walk-forward 结果（HPC 实跑，已回填文档）
- 4 指标 verdict：**Peak Hit 0.25 ❌ / Timing 0.0 ✅ / Peak Intensity 12.13% ✅ / Direction 0.672 ✅**。
- Peak Hit 短板：4 真峰只抓 1（漏 3）；其余 3 项达标。
- 产物已落盘本机：`results/ili/miflu_fulltest_walkforward.csv` + `figures/fig_miflu_eval_continuous.png` + `figures/fig_miflu_eval_diagnostics.png` + `metrics/miflu_verdict_table.md` + `miflu_ili_peak_indicators.csv`。
- `MIFlu 复现障碍分析.md` Q1/Q5 的"待回填"已全部填入真实数字。
