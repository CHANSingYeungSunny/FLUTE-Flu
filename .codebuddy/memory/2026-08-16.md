# 2026-08-16 — MIFlu Phase-1 Gate 执行记录

## 完成事项（只读审计 + Gate 四项）
- HPC 现可达（登录节点 `hpclogin02`；旧 `burgundy` 主机名已改为登录节点别名）。
- 远程审计完成：远端 `scripts/` 仅有 ChatTime-1-7B-Chat 的 L=52 walk-forward 产线
  (`submit_ili_fulltest.sh` / `run_ili_walkforward.py` / `make_ili_fulltest_figure.py` /
  `compute_peak_metrics.py`)，与本地 MIFlu `train_miflu.py` 系是**两条完全独立产线**。
  本地 MIFlu 脚本此前从未上传到 HPC。

## Gate 四项（已全部打勾，待用户授权后才提交 sbatch）
1. ✅ 上传 7 文件到远端 `scripts/`：`train_miflu.py`(L_list=[24,36,48,60])、`miflu_model.py`、
   `textual_embedder.py`、`compute_miflu_indicators.py`、`make_miflu_evaluation_figure.py`、
   `feature_verification.py`、`submit_miflu_paper_protocol.sh`（均 2026-08-16_17:57）。
2. ✅ 新建 `scripts/submit_miflu_paper_protocol.sh`：stingy + gpu:a100:1 + time=04:00:00 +
   PATH-prepend conda(mlflu_hpc) + HF cache(gpfs1 scratch)。支持 `MIFLU_HORIZON=L` 单档提交，
   四档独立循环：`for L in 24 36 48 60; do MIFLU_HORIZON=$L sbatch .../submit_miflu_paper_protocol.sh; done`。
3. ✅ 远端 4 个旧脚本归档为 `*_DEPRECATED`（不删除）：`submit_ili_fulltest_DEPRECATED.sh`、
   `scripts/run_ili_walkforward_DEPRECATED.py`、`make_ili_fulltest_figure_DEPRECATED.py`、
   `compute_peak_metrics_DEPRECATED.py`。保留作 Phase 2 ChatTime 对照参考。
4. ✅ 本地+远端两份 `HPC_GUIDE.md` §6 重写为 Phase-1 协议（L=24/36/48/60 四档独立 sweep 为权威），
   并补 GOTCHA（2026-08-16 本地改动未同步远端导致两产线分叉的教训 + 检查清单）。
5. ✅ 远端 `PROJECT_STATE.md` 第 7 行 "MIFlu reproduction: ABANDONED" 改写为
   "NOT pursuing bit-exact reproduction ... GOAL = protocol alignment + magnitude/trend consistency"，
   并同步改写 CUT LIST 中同名条目。两处均不再含 "ABANDONED" 词。

## 待办（用户确认后）
- 用户看到"4 项全部打勾"并授权后，才提交 4 个独立 `stingy` 训练任务（单任务 ≤4h）。
- 注意：提交前务必确认 `MIFLU_HORIZON` 循环覆盖 24/36/48/60，且远端无残留非 DEPRECATED 的旧 ChatTime 提交脚本。

## 工具备注
- PowerShell 下 `scp`/`ssh` 对单引号与 CRLF 敏感；可靠做法：本地写 LF 脚本 → `scp` 到远端 → `ssh host bash /path/script.sh`。
- base64 管道法也行（避免 BOM/CRLF）。

---

## 晚间续做（2026-08-16 晚）：四档训练 + Master Brief 核查

### HPC 连接
- 用户中途关过 VPN 导致连不上；恢复后确认 `burgundy.hpc.cityu.edu.hk` 可达（→ hpclogin02）。
- 排查发现 PowerShell `ssh host "MIFLU_HORIZON=24 bash -c '...'"` 会把 `$MIFLU_HORIZON` 在本地展开成空。
  **正确做法**：`sbatch --export=ALL,MIFLU_HORIZON=24 scripts/submit_...sh`（sbatch 自己注入，不受本地 shell 影响）。
- 分区对比：所有人抢 `gpu_a100` 分区 → 反而难拿卡；`stingy` 分区里有 `gpu-a100-04` 整节点 idle。
  **结论**：坚持用 `stingy` 分区投 A100（低优先级但资源常空着），不要投 `gpu_a100`。

### 训练提交史（已纠正）
- 506504（漏传 horizon→会跑全 sweep）、506515（同样漏传）、506516（gpu_a100 难拿）、506518（gpu_a100）→ 全部取消。
- 506519 = stingy + `MIFLU_HORIZON=24`（用 --export 注入），**已 RUNNING 并跑完 L=24 全部 10 rep**。
- **506519 是在 seed bug 下提交的 → 结果无效**：10 rep `test_mse` 全 = 2.360905（bit-identical，std=0）。

### Step 1–4 核查结论（Master Brief）
- **Step 1 seed bug 确认**：`train_miflu.py:356` 原 `seed=args.seed`，`train_L` 内 `manual_seed(seed+L)` 不含 rep。
  **已修复** → `seed=args.seed + rep`（方案 A，用户确认：论文未规定 seed 方案，选常规易描述的 A）。
  本地改完 `py_compile` OK → 已 scp 到 HPC `scripts/train_miflu.py` → HPC 端 `grep`+`py_compile` 确认生效。
- **Step 2 PE/LayerNorm**：`miflu_model.py` 中 `wpe` `freeze=False`（可训练）、`ln_1/ln_2` `requires_grad=True`（可训练）、
  attention 基础权重冻结 + LoRA 可训练、mlp 冻结。**与论文 IV-C 完全一致，无 bug**。推翻了"可训练参数比论文少"的假设。
- **Step 3 文本 embedder**：`textual_embedder.py` 用完整 12 层 GPT2（`GPT2Model.from_pretrained("gpt2")`），独立实例、冻结、不截断、不共享 K=6 权重 → 与论文一致。
- **Step 4 prompt 模板**：`PROMPT_TEMPLATE_7` 任务指令 = `"Predict the next {L} steps given the previous 104 steps for the information attached."` 与 Table X 逐字一致；变量 7(OT) 描述含 "peaks 2 times...upward" 与论文一致 → 无需改。

### 下一步（待用户确认 Step 7）
- 仅 Step 1 需改代码，已改已上传。Step 2/3/4 均正确无需改。
- 统一 retrain：stingy 顺序提交 L=24/36/48/60（不单独重跑 L=24；之前 506519 作废）。
- 提交命令模板：`sbatch --partition=stingy --export=ALL,MIFLU_HORIZON=L scripts/submit_miflu_paper_protocol.sh`
- 提交后监控：每 rep `test_mse/pcc` 应**不再** bit-identical（验证 seed fix 生效）；出现 NaN/Inf 先报告。
