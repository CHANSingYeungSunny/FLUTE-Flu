# 2026-08-18

## 用户核心诉求
- 用户 FYP：复现 MIFlu（IEEE JBHI 2025）并创新发顶刊。导师要求先完全理解论文、下周用中文讲清随机问题，并改代码严格按论文复现。
- 用户明确：只需回答导师实际问的 7 个问题，无关内容删掉；图左准右差与 Timing=0.0 的**根因都是误用 ChatTime walk-forward 协议（hist=104/pred=52/stride=52）**，不是泄漏、不是峰值检测细节 bug。
- 用户要求：第 8 部分（行动清单）删掉，由 agent 自己执行。

## 本次执行（文档勘误 + 交叉参考统一，无重训/无删 PDF）
- 重写 `MIFlu_论文完整解析与答辩解答.md`：仅保留导师 7 问（I/O 含 7 异质通道+L=24 澄清、2019-07-01 输入、70:10:20、Providers 倒果为因、偏差溯源、图左准右差、Timing=0.0），删除原九问无关部分；第 6/7 部分直接点明 ChatTime 协议错配根因；删除原第 8 行动清单。
- 修正两处事实错误：撤回 "patch 池化破坏时间结构" 误判（miflu_model.py:334 是 GPT4TS 标准复刻）；替换 stale 指标数字（旧 1.30x/4–8.6× → 当前 L=24 MSE=5.056/MAE=1.500，vs 论文 1.542/0.726 = 3.28×/2.07×）。
- `scripts/train_miflu.py` L19/L329：删除文件引用改为 `MIFlu_paper.md`。
- `docs/scaler_convention_supplement.md`：修正错误 A（线性缩放均匀，无"压缩大误差更狠"）、错误 B（CDC 区间回溯修订未排除，非 ruled out/moot）；pooled 降为未确认部分解释。
- `MIFlu 复现障碍分析.md`：L4 评估协议改论文协议、Q2 改四档、Q5 旧表标注+撤回 patch 误判+用当前数字、Output 改静态切分。
- `PROJECT_STATUS.md` L158：撤回 patch 误判，用当前数字。
- 英文 `CodeBuddy_Reproduction_Fix_Brief_EN.md` 已不存在（路径搜索 0 结果），其 §1 旧引用无需处理；train_miflu.py 残留引用已清。

## 搜索结论（用户要求彻底搜）
- MIFlu 全网无公开代码、无第三方成功复现记录、无人基于它创新发表。最接近平行工作：licx11/LLMs4Influenza（Cell Reports Methods 2026，开源但非 MIFlu 复现，设定不同）。
- 已向用户诚实说明：不能"复现后创新"，只能"以 MIFlu 为架构启发 + 开源基线（GPT4TS/PatchTST）+ 自创新模块"路线。

## 后续追加执行
- **删除 `MIFlu 复现障碍分析.md`**：因内容已并入 Q&A 文件，且无信息丢失；OT 推导与七通道解释已补进 `MIFlu_论文完整解析与答辩解答.md` §1。
- **修补删除后引用**：`docs/ILI_FOUR_INDICATORS.md` L9 指针改指向 `MIFlu_论文完整解析与答辩解答.md` §1；`docs/full_repo_audit_report.md` L31 标注为已删除并入 Q&A。
- **Q2 具体到 epiweek/日期**：预测 2019-07-01（epiweek 201112）时，数字输入 = epiweek 200913–201111（约 2017-07-04–2019-06-24），文字输入统计量同该 104 周，输出 = epiweek 201112–201135（约 2019-06-25–2019-12-17）。已同步到中英两个 Q&A 文件。
- **Q4 生成两张证明图**：
  - `scripts/make_q4_causality_figures.py`：双向 Granger 因果图 + ILITOTAL-vs-PROVIDERS 散点回归图。
  - 关键结果：PROVIDERS→ILITOTAL p=1.2×10⁻⁶（单向显著），ILITOTAL→PROVIDERS p=0.11（不显著）；散点 R²=0.496，r=0.704。
  - 结论因此改为 nuanced：不是纯倒果为因，也不是纯流行病学主因，而是"真实领先信息 + 监测口径/采样规模效应"的混合。
  - 输出：`results/ili/figures/q4_granger_bidirectional.png`、`q4_ilittotal_vs_providers_scatter.png`、`data/q4_causality_summary.csv`。

## 待办（未执行）
- 远端 `$HOME/scratch/CHATTIME/Chattime/PROJECT_STATE.md` SSH 确认旧引用（需 HPC 登录）。
- `.codebuddy/plans/MLFlu文档精简与文件清理_3e491920.md` L31/L65 加注。
- Option 3 CDC 区间内回溯修订的具体数据核对（目前仅文档层面标注为未排除候选）。

## 交叉参考统一勘误（plan 执行，2026-08-18 续）
- **确认 `MIFlu_paper.md` = 完整 PDF 转换**（Abstract、I–VII、Tables V/VI/VII/VIII 逐字存在），唯一权威交叉参考源地位不变。
- **`MIFlu 复现障碍分析.md` 已在前序会话删除**，其内容已并入中英 Q&A，故原 plan 的该文件 [MODIFY] 步骤视为 superseded（无残留旧协议/旧提取引用，Q&A 已正确指向 MIFlu_paper.md）。
- **code-explorer 子代理全量审计**：残留陈旧引用共 4 处，已全部订正：
  1. `data/training_miflu_log_20260816_173454.txt:3` — 日志 "PAPER PROTOCOL (MIFlu_Complete_Extraction.md)" → 改指向 `MIFlu_paper.md`（注明旧文件已删）。
  2. `scripts/chattime_variant/generate_walkforward.py:15` 与 `:56` — OT "black box, best guess" → 改为当前约定（OT=num_patients 全量、论文仅称 'OT' feature、非黑箱）。
  3. `MIFlu_paper_analysis_and_defense_EN.md:133` — 把 CDC 数据版本列为 "Ruled out" → 修正为"2024+ 假设证伪，但 2002–2021 in-span 回溯修订仍 UNEXCLUDED (Option 3)"。
  4. `MIFlu_论文完整解析与答辩解答.md:145` — 同步修正中文版同样的 "已排除" 过度结论。
- **`.codebuddy/plans/MLFlu文档精简与文件清理_3e491920.md` L31/L65** 已加注旧提取文件已删、现统一指向 `MIFlu_paper.md`。
- **scaler_convention_supplement.md** 经查已在前序会话完成勘误（线性缩放数学正确、CDC "NOT ruled out"、pooled 降为未确认部分解释），本次无需再改。
- **Option 3 CDC 区间内回溯修订**：文档层面已标注为未排除候选（与 scaler doc 一致）；具体数据核对留待 HPC 或本地 CSV 深核（当前仅结论层）。
- **远端 `PROJECT_STATE.md` 仍未处理**：本机无法解析 `hpclogin02`（DNS 失败，疑似未连 VPN/校园网），SSH 不可达。待联网后执行。
- 类别 2（L=52 当主协议）/类别 5（instance-norm 旧提取衍生）经审计**无残留违规**，相关文档已正确标记 DEPRECATED/ISOLATED 与 PARTIAL UNCONFIRMED。

## HPC 登录主机名更正（用户指示：hpclogin02 是错的）
- 用户明确指出 **`hpclogin02` 作为连接目标是错误的**，应从 `HPC_GUIDE.md` 删除。
- **正确登录 FQDN = `burgundy.hpc.cityu.edu.hk`**（备份 IP `144.214.138.99`）。bare `hpclogin02` 在本机 DNS 无法解析（需 VPN + FQDN）。SSH alias `cityu` → 该 FQDN。
- 已执行：从 `HPC_GUIDE.md` §1/L8、§13/L348、§13/L353 删除所有 bare `hpclogin02`（"lands on hpclogin02"、"round-robined to hpclogin0X"）。保留正确 FQDN。
- 同步清理 `docs/full_repo_audit_report.md`（2 处 hpclogin02 → FQDN）。
- 更正 `MEMORY.md` HPC 段：原"Login node: hpclogin02，old burgundy deprecated" **错误**，改为"Login FQDN: burgundy.hpc.cityu.edu.hk；hpclogin02 WRONG/deprecated，勿用"。
- **远端 `PROJECT_STATE.md` 现已可达**（用 FQDN 连接成功，内部显示名确为 hpclogin02，但文件本身已写明"bare hpclogin02 does NOT resolve"）。只读检索：该文件仅 L61/L75 以"post-SSH 内部显示名"语境提及 hpclogin02（正确语境），L260 正确标注旧提取文件已删。**远端文件无需改动**，旧提取文件引用已正确声明为已删。交叉参考统一任务全部完成。

## Q4 重做（简单叠加图为主证 + 顶刊口径；Granger 降附录）
- 用户要求：全部重做 Q4，简单 GT 叠加图为主证，Granger/散点降为附录，补"顶刊如何证明采样框效应"口径；X 轴选"top journal version"（全序列 2002–2021 证明每年冬季共动）。
- **新增 `scripts/make_q4_overlay_figure.py`**：读 `data/raw/national_illness_raw.csv`（1025 周），双 y 轴画 GT ILITOTAL（蓝,左轴）vs NUM. OF PROVIDERS（橙,右轴），epiweek→年份 x 轴，流感季黄色阴影，输出 `results/ili/figures/q4_overlay_ilitotal_vs_providers.png`（已生成，228KB，覆盖 200201–202152）。
- **重写中文 `MIFlu_论文完整解析与答辩解答.md` 第 4 部分（原 L88–129）**：一句话结论置顶 → 主证叠加图 → 附录（Granger 图A + 散点图B + 数字）→ 综合结论原话 → 顶刊为何认这套（分母/采样框效应 + Granger 先导区分真实信号）。
- **同步重写英文 `MIFlu_paper_analysis_and_defense_EN.md` §4（原 L79–120）**：结构对齐、图标签英文、顶刊口径英文。
- 关键数字（不变）：PROVIDERS→ILITOTAL Granger p=1.2e-6（单向显著），反向 p=0.11；散点 R2=0.496, r=0.704, OLS 斜率≈12.35。
- 未触发协议变更硬规则（仅文档 + 新 figure 脚本）；未重训、未删 PDF、未覆盖旧 Q4 产物（保留 q4_granger_bidirectional.png / q4_ilittotal_vs_providers_scatter.png）。

## Q4 主证图 bug 修复（<matplotlib.patches.Path object> 泄漏）
- 用户发现主证图图例出现 `<matplotlib.patches.Path object at 0x...>` 这种无人能懂的对象 repr 文字。
- 根因：`scripts/make_q4_overlay_figure.py` 原把 `season_patch = Patch(...)` 同时作为 handle 和 label 传入 `ax1.legend(...)`，matplotlib 渲染其 `__repr__` 为图例文字。
- 修复：删除 `from matplotlib.patches import Patch` 导入与 `season_patch` 变量，legend 改为 `ax1.legend(h1 + h2, l1 + l2, ...)` 仅两条线；**保留** `axvspan` 流感季黄色阴影（用户不要删阴影，只删图内提到它的文字/对象 repr）。
- 已重跑重新生成 `results/ili/figures/q4_overlay_ilitotal_vs_providers.png`（217KB）。脚本内已无 Patch/matplotlib.patches/season_patch 残留。文档无需改。

## Q4 主图段叙事调整（主图直接作答，附录转为支撑）
- 用户最新要求：对导师讲法 = **用主图趋势直接回答"PROVIDERS 是主因还是倒果为因"**，再用另两张附录图（Granger + 散点）**解释/支撑**该结论（而非主图只展示规律、结论留到附录后）。
- 已重写中英文 Q&A 主图段：主图"读图"现在直接给结论（既非纯倒果——因每年同步甚至略领先起峰；也非流行病学主因——因 ILITOTAL 定义就是 providers 汇总计数，共动是定义层面机械关系），并明确"主图已给出答案"；附录标题改为"支撑/解释主图结论"。
- 未触发协议变更；仅文档措辞调整，图与数字不变（Granger p=1.2e-6 / r=0.704 / OLS≈12.35）。

## Q4 措辞统一：固定用"真实领先信息与监测口径效应的混合"
- 用户要求全篇统一术语，不要时而"真实领先信息与监测口径效应的混合"、时而"定义性汇总计数带来的共动 + 一点真实领先信息"。
- 已将中英文主图段结论统一为：CN「真实领先信息与监测口径效应的混合」；EN「a mix of real lead information and sampling-frame (denominator) effect」。综合结论原话段本就用语一致，无需改。
- 该短语含义（给用户讲解用）：①监测口径效应=ILITOTAL 定义上就是 providers 上报病例的汇总计数，故二者共动是定义/核算层面的机械关系，非因果；②真实领先信息=Granger 显示 PROVIDERS 单向领先 ILITOTAL（p=1.2e-6），携带一点真实预测信号。混合=模型看重 PROVIDERS 是这两部分叠加，不能称"provider 导致流感"。

## Q4 大白话解释融入正文（非独立附块）
- 用户进一步要求：不要把口语化讲解作为文末独立"附："块追加，而要**自然融入段落**（主图读图段讲"监测口径效应/为什么共动不是因果"，附录段讲"真实领先信息/混合=两层叠加/对导师一句话"，散点段点明即图A口径效应的截面体现）。
- 已重写中英文 Q4 段：删除独立的"附：怎么向导师用大白话解释…"块，内容拆分融入主图 reading + 附录，行文连贯。图与数字不变，未触发协议变更。

## Q4 图内嵌 md
- 用户要求把对应图直接嵌入 md（而非仅写路径）。已在中英文 Q&A 的 Q4 三处图引用后插入 `![](results/ili/figures/q4_overlay_ilitotal_vs_providers.png)`、`q4_granger_bidirectional.png`、`q4_ilittotal_vs_providers_scatter.png`，相对路径从仓库根解析（docs 在根目录）。标准 Markdown 图片语法，VS Code 预览/GitHub/Typora 均可渲染。

## 各问题相关图内嵌（扩展到 Q5/Q6/Q7）
- 用户要求"每个相关问题都嵌入对应图"。已扩展：
  - Q5（National 偏差溯源）→ 嵌入 `fig_miflu_eval_diagnostics_L24.png`（各通道/误差分解）。
  - Q6（左准右差/混用数据）→ 嵌入 `fig_miflu_eval_continuous_L24.png`（GT 虚线 vs 预测实线，左准右差直观）。
  - Q7（Timing/峰值）→ 不重复嵌图，加一句指针指向 Q6 已嵌的连续图（避免重复大图）。
  - Q1–Q3、Q1 输入通道说明等无本地生成 PNG（仅引用 MIFlu_paper.md 的论文图，PDF 派生不可嵌），故不嵌。
- 未触发协议变更；仅 md 图片内嵌。
