# 2026-08-06

## HPC Deployment (CityUHK Burgundy) — full pipeline completed
Deployed MLFlu training + figure generation to Burgundy HPC. All 4 horizons (L=24/36/48/60) trained and figured; results downloaded to local `hpc_results/data/`.

Key fixes applied during run:
- Scripts: `module unload default && module load old_modules`; `anaconda3`→`miniconda/python-3.9` (final: absolute PATH conda activation `export PATH=/home/sychan552/.conda/envs/mlflu_hpc/bin:$PATH`).
- SBATCH `--output`/`--error`: absolute paths (was `$HOME/...` which didn't expand → nested literal `$HOME` dir).
- `make_forecast_figure.py`: Peak Ledger (Fig B2) added; output filenames suffixed `_L{L}`; fixed `falsep` epiweek-as-index IndexError.
- L=36 first attempt (491036) NaN due to concurrent training on same GPU; re-run alone (491106) succeeded.

Long-term notes saved to MEMORY.md.
