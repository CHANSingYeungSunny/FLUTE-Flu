# 2026-08-17

## L=36/48/60 提交前置收尾（已完成文档层）
- **撤回 CDC-revision 归因**：`REPRODUCTION_PROOF_0d1869c0.md`（plan 文件）原将 National MSE/MAE gap
  部分归因于"CDC 2024→2026 回溯修订"，现顶部加 CORRECTION 块并改写 line 39/65 归因，撤回该说法。
  依据：`verification_report.txt` 与 `miflu_ground_truth.json` 均已删除（2026-08-17），且其依赖的旧
  `MIFlu_Complete_Extraction.md` 含编造内容；本地数据仅 `epiweek 200201..202152`，无 2024+ 周次，
  vintage 假设不成立。
- **`docs/scaler_convention_supplement.md` 改框架**：由"两候选并行解释"改为已结算口径——
  pooled-fit 解释大部分 MSE（5.056→≈1.61，≈4% of paper，但 MAE 仍 ≈0.85 vs paper 0.726，~17% 残差）；
  CDC-vintage **ruled out / moot**；残差 MAE ~17% 与 train-only 主数 ~2× **unexplained**（最可能为论文
  未披露的 scaler 拟合约定 + 架构/超参差异），诚实声明非完美复现。L=36/48/60 提交闸门满足（train-only
  仍为主口径，无协议改动）。
- **`train_miflu.py` MSE/MAE-only 输出裁剪确认在位**（前次会话已完成）：`pcc_ili` 仍在 `evaluate()`
  计算并用于 NaN-rep 有限性检查（line 215/228），但 CSV 行/聚合表/日志表仅含 MSE/MAE。
- MEMORY.md line 51 OT 描述从"black-box best guess"改为"我们自己的实现选择"，与 textual_embedder.py 修订对齐。

## ⚠️ 阻塞：VPN 未连接
- `hpclogin02` 无法解析（DNS 失败）；OpenVPN 适配器存在但无活动隧道。无法 scp/sbatch 提交 L=36/48/60。
- 待用户连上 VPN 后，执行：
  `for L in 36 48 60; do MIFLU_HORIZON=$L sbatch /home/sychan552/scratch/CHATTIME/Chattime/submit_miflu_paper_protocol.sh; done`
  （每 L 独立 stingy A100 1卡 4h 作业；注意避免同节点并发多个训练，见 Bugs Fixed #1）
- 注意：MEMORY 记录本地脚本已含 MSE/MAE-only 修复并"已上传 HPC"，但本次因 VPN 断未能二次确认远端
  train_miflu.py 内容；提交前应在 VPN 恢复后 `ssh` 验证远端脚本确实为裁剪后版本（独立校验习惯）。

## HPC 提交进展
- L=36 已提交（job **506612**），状态 PD（Pending, Resources — 等 GPU）。
- L=48/60 因 QOS 限制（max 1 pending job per user）暂无法提交，需等 L=36 进入 R 状态后依次提交。
- 已改用正确 FQDN `burgundy.hpc.cityu.edu.hk`（非 `hpclogin02`）。
- 远端 `train_miflu.py` 确认为 MSE/MAE-only 裁剪版（`pcc_ili` 仅在 evaluate() 内部用于 NaN-rep guard，不写入 CSV/表）。
- **轮询策略已调整**：不再紧密轮询 squeue，改为 ≥10-15min 间隔，仅在 L=36→R 时提交 L=48，L=48→R 时提交 L=60。

## 任务 A 完成：L=24 诊断图 + 图注
- 从 HPC 拉取 `predictions_miflu_L24_paper_protocol.csv`（1.1MB，test rows=4368）到本地 data/。
- 增强 `make_miflu_evaluation_figure.py` 主路径：
  - 真正调用 `compute_miflu_indicators` 计算真实 4 指标（原为 NaN 占位）
  - annotation box 加 "SUPPLEMENTARY indicators (non-MIFlu-paper-protocol; borrowed from ChatTime)" 声明
  - Timing/Peak Intensity 带 hit count `(based on 2/4 matched peaks)`
  - continuous 图加 val|test 分界线（abs_week=819），标题写 "TEST SET ONLY"
- 生成产物：
  - `results/ili/figures/fig_miflu_eval_continuous_L24.png`
  - `results/ili/figures/fig_miflu_eval_diagnostics_L24.png`
  - `results/ili/figures/FIGURE_CAPTION_L24.md`（完整图注，含 leakage audit 证据回应"是否混数据"质疑）
- L=24 test-only 指标：Peak Hit=0.50(2/4), Timing=0.5wk, Peak Int.=31.7%, Direction=0.70

## 任务 B 定稿（v3）：按 §7 原话 10 问重排
- 用户提供 §7 逐字原文（10 条导师原话，不在磁盘，直接粘贴）。已按此重排 `docs/Tutor_QA_Draft_2026-08-17.md`：
  1.Why L=24(and T=104) 2.What exact input predicts a date 3.Train/test mixed suspicion
  4.Why Timing=0.0 5.Peak hit/timing/intensity narrative+business implications
  6.Justify 70:10:20 7.NUM.OF PROVIDERS real driver or reverse-causation
  8.National MSE/MAE deviation(核心未解项) 9.input/process/output+7 heterogeneous channels ELI5
  10.Correct hist=104/pred=52/stride=52 wrong claim
- MoE/UCS 完全删除（非导师问题）。指标全用真实值。

## ChatTime 归因移除（4 指标来源纠错）
- 用户指出：4 诊断指标来自本项目自有脚本命名，非 ChatTime 论文方法论 → 移除 "borrowed from ChatTime"/"借自 ChatTime" 归因。
- 已改：`compute_miflu_indicators.py`（header + L299 打印）、`make_miflu_evaluation_figure.py`（docstring + annotation box）、`MIFlu 复现障碍分析.md`（Q1）、`docs/ILI_FOUR_INDICATORS.md`、`FIGURE_CAPTION_L24.md`。
- 保留：ChatTime 指代 deprecated L=52 walk-forward 管线本身（准确，应留）。
- annotation box 简化：删除 "MIFlu National paper protocol reports MSE/MAE only" 句，仅留 "Supplementary indicators:" 标签 + hit-count 括号（仍保留）。full caveat 留在 FIGURE_CAPTION_L24.md。

## 图尺寸/溢出修复
- 连续图标题缩短为 "MIFlu Forecast vs Ground Truth — Test Set Only, L=24"（fontsize 10.5）。
- 程序化检查（renderer extent）：连续图 700x420px 标题右缘 581<700 无裁切；诊断图 1300x420px 两标题均无裁切；无 artist 溢出画布。
- 两图已重新生成（continuous 312KB / diagnostics 183KB @ 2026-08-17 3:17）。

## HPC 状态
- L=36 (506612) 仍 Pending；维持 ≥10-15min 轮询间隔，L=36→R 即提交 L=48，L=48→R 即 L=60。
- 任务 C（Regional Table VI 缺口清单）待三任务全入队后做。

## 待办
- [HPC] 等 L=36 → R 后提交 L=48，再等 L=48 → R 后提交 L=60
- [任务 C] 三任务全部入队后评估 Regional (Table VI) 复现缺口清单

## 🌙 夜班轮（用户睡前最终指令）
- **第一步 OT 文档修正 — 已完成并 grep 确认**：全项目搜索 `num_patients/100|除以100|/100|OT.*100|scale.*100` 仅命中 `train_miflu.py` 第36行*解释性注释*（"训练代码从未做 /100 缩放"），无任何旧口径生效代码/文档残留。三权威文件 + MEMORY + 日志此前已改全量口径 + 保留免责声明 + Q9 核实说明（含"不影响已训练模型正确性"收尾）。
- **第二步 HPC 提交 — 关键事实纠正**：
  - `sacct` 核实：**L=24 (best L24.pth 00:29) 与 L=36 (506612, 04:13 COMPLETED, 30min) 均已完成**。此前 summary 记的"L=36 pending"是过时信息。
  - **L=48 已提交并在运行** → job **506643**，节点 gpu-a100-05，~04:14 R。
  - **L=60 因 QOS 1-pending 限制无法提前排**：实测 `sbatch --array=0-1%1` 被 **`QOSMaxSubmitJobPerUserLimit`** 拒绝（EXIT=1）；`--dependency=afterany` 链在 L=48 仍 pending 时同样被拒（占满唯一 pending slot）。
  - **解决**：本地后台 PowerShell 进程（`scripts/_autosubmit_L60.ps1`, PID 42788）每 120s 检测 506643，一旦离开队列立即 `sbatch L=60`。用户无需熬夜。日志：`scripts/_autosubmit_L60.log`。
  - 远端 `train_miflu.py` 独立核实 = MSE/MAE-only 裁剪版（epochs=20, pcc_ili 仅 NaN-guard），符合用户要求。
  - **reason code 精确记录**：`QOSMaxSubmitJobPerUserLimit` 属 QOS(Resource)Limit 类下的 Max Submit Jobs Per User 子限制（stingy QOS: Jobs=1, Submit Job=1）。已写入 HPC_GUIDE §11。
- **HPC_GUIDE.md 更新（本地+远端一致）**：新增 §10 资源红线（官方原话+禁止整节点/多卡/超长time）、§11 QOS reason code（实测 QOSMaxSubmitJobPerUserLimit 精确定位 + showqos/sacct 诊断法 + backfilling 提示）、§12 scratch 无备份→每 horizon 完成必须 scp 回本地、§13 登录方式（burgundy FQDN + 备用 IP 144.214.138.99）。
- **第三步 汇报频率**：保持 ≥30min 节律，仅汇报 NaN/失败、三档全完成、或 QOS 强制手动三种情况。
- **第四步 Task C**：缺口清单已于前次会话写入 `docs/REGIONAL_TABLE_VI_GAP_ANALYSIS.md`，方案 A 合理，用户确认暂缓补脚本。
- **待明早确认**：L=48 (506643) 完成 + L=60 是否由后台进程成功提交并完成（查 `_autosubmit_L60.log` + `sacct`）。完成后按 §12 拉取 L48/L60 checkpoint + 预测 CSV 回本地。

## 任务 C 完成：Regional (Table VI) 缺口清单
- 交付物：`docs/REGIONAL_TABLE_VI_GAP_ANALYSIS.md`（只读分析，未改任何代码）。
- **已具备**：`download_us_region.py` / `data/raw/us_region_raw.csv` / `verify_us_region.py`（50:10:40 vs Table III）/ `train_regional_miflu.py`（T=20,N=10,L_list 7档,K=4,Lp=4,RMSE/PCC de-normalized）/ `train_regional_baseline.py`（GPT4TS）/ `miflu_model.py`（已支持 N=10,T=20）。
- **8 大缺口**：
  1. 6 个对比模型未实现（ReILIF/TFT/SAIFlu-Net/Cola-GNN/STNN/CNNRNN）——Table VI 需 8 列，现仅 MIFlu+GPT4TS；论文说 region 对比取自 [14]/[9]，可引用原值但需标注非本仓库复现。
  2. ReILIF 外生数据（wind/temp/pop）缺失，`us_region_raw.csv` 仅 num_ili/patients/providers。
  3. Regional prompt 模板未逐字核对论文 Table I；`build_regional_prompt` 有 dead code（train_min/max 未用），min/max 重算绕开 train_mean/std 有口径漂移风险。
  4. 无 Regional 切分 leakage audit 脚本（National 有 audit_channel_isolation.py）。
  5. 无 Regional figure（Figure 4, L=2/L=20）/ 指标脚本。
  6. 无 Regional HPC SLURM 提交脚本（受 QOS 1-pending 限制，需 --dependency 链式）。
  7. 无 Regional 结果聚合/对照表脚本（Table VI 格式 + 相对增益 26.2%/43.0%）。
  8. Regional ablation 非阻塞（论文 Table VIII 仅 National）。
- **推荐方案 A**：自跑 MIFlu+GPT4TS 两列 + 引用其余 6 列原论文值（标注来源）；补缺口 3/4/5/6/7 约 5 脚本。方案 B（全 8 列）工作量过大不推荐。

## 🔧 晚间收尾：两处硬伤堵上 + 图重出 + provenance 核查

### 1. 旧无后缀图隔离（问题1）
- 发现 `results/ili/figures/fig_miflu_eval_continuous.png` + `fig_miflu_eval_diagnostics.png`（无 `_L{L}` 后缀，旧协议 Peak Hit=0.25/"best guess" 标记）仍在主目录。
- 已移动到 `figures/deprecated/fig_miflu_eval_continuous_OLD_PROTOCOL.png` + `fig_miflu_eval_diagnostics_OLD_PROTOCOL.png`。
- **根因**：grep 确认当前 `make_miflu_evaluation_figure.py` 只写 `_L{L}` 或 `_DEPRECATED` 后缀，无脚本写无后缀文件名 → 旧文件是历史遗留，非本次生成，根因已从源头排除（不需改脚本默认路径）。

### 2. Regional Table VI 命名纠错（问题2）
- `Regional_Table_VI_summary.{csv,md}` 内容实为 **National 四档（L=24/36/48/60）MSE/MAE+4补充指标**，对应论文 **Table V**，非 Regional（Table VI，10 HHS regions，未训练）。
- 已重命名为 `results/ili/National_Table_V_summary.{csv,md}`；脚本 `summarize_regional_table_vi.py` 路径+注释同步改 National/Table V。
- **全项目 grep**：`Regional_Table_VI_summary` 0 命中；文档中 "Regional Table VI" 均指真实 Regional 任务本身（正确用法），无把 National 误称。
- **撤回历史误记**：`PROJECT_STATUS.md` 原 line 180-181 + 2.2 节把 Regional 标"✅ 已跑 CSV 真实"——核实 `results_regional_miflu.csv` **不存在**（0 命中），Regional 从未训练。已改"❌ 未训练/CSV 缺失"，与 `REGIONAL_TABLE_VI_GAP_ANALYSIS.md` 缺口清单对齐。

### 3. 图例重叠修复（用户指定）
- `make_miflu_evaluation_figure.py` `build_continuous_panel`：val|test 标注原在 y 顶（`va="top"`）与左上图例重叠；改为轴底竖排 + 图例 `bbox_to_anchor=(0.0,0.98)` 微下移。4 张连续图已重出（诊断图无此问题未动）。

### 4. Step 8 对照表（National MSE/MAE vs 论文 Table V）
- 我们的 10-rep mean±std：L24 MSE=5.056±1.583 / MAE=1.500±0.121；L36 6.834±0.937 / 1.680±0.093；L48 6.405±0.912 / 1.651±0.081；L60 7.070±0.724 / 1.701±0.074。
- 论文 Table V：1.542/0.726, 1.422/0.779, 1.414/0.757, 1.364/0.719。
- **结论**：MSE 偏高 3.3–5.2x，MAE 偏高 ~2x，std 在 L=24 占 31% → 数量级偏差明显，未达论文精度；趋势方向正确（L↑ MSE/MAE 略增）。待查归一化 vs 原始空间口径。

### 5. Provenance 三问（用户质疑地基可靠性）
- 背景：用户担心"best_rep['rep_ckpt'] 路径 bug"若在本轮诊断前就存在，则 MSE=5.056 那组数字地基不稳。
- 代码逻辑：`train_miflu.py` L367 `os.replace` 后 L368 `best_rep["rep_ckpt"]=final_ckpt`（修复行）；L374 `export_prediction_csv` 用此路径 `torch.load`。**若缺 L368，export 必崩、CSV 不生成**。
- 反证：本次 HPC L=24 (job 506576, 08-17 00:32) 的 `predictions_miflu_L24_paper_protocol.csv` 存在且能被图脚本正常读 → 训练时代码**已含 L368 修复**。
- **结论**：(1) 修复不晚于 08-17 00:32 训练；(2) 早前诊断与今晚出图用同一份 HPC 产物（job 506576）；(3) MSE=5.056/MAE=1.500 及 pooled-fit 换算**不需重算**。

### 6. 跨档峰值漏检一致性（导师第 5 问素材）
- 4 档 peak indicators 实证：pk1(idx54)/pk2(idx105) **全档 Hit**；pk3(idx146, 2019-20 异常大峰)/pk4(idx186, 2021 小峰) **全档 Missed**。
- 推论：非误差累积（否则 L=60 应漏更多），是 **OOD 信息缺口**（训练 2002-2019 无 COVID 异常峰先例）→ 无论给多长历史窗口都补不上。比"Peak Hit=0.50"更有说服力。
- 已写入 `FIGURE_CAPTION_L24.md` 新增节「跨档一致性发现」。

### 7. 空间口径澄清 + Pre-COVID 验证（回答"为何比之前聊的差距大"）
- **CSV/prediction_ili 空间确认**：`export_prediction_csv` (train_miflu.py L277-279) 写 `yh_rev`（=RevIN 逆变换回 StandardScaler 空间），`gt=by` 来自 `data_norm`（StandardScaler 空间）→ CSV 两列都是 **StandardScaler 归一化空间**。`evaluate()` (L153-154) 同理用 `yh_rev` vs `Y_norm`，主表 MSE/MAE 也是归一化空间。论文 Table V 同为 StandardScaler 空间（miflu_model.py L455-456 注释 "metrics computed on StandardScaler-normalized data"）。**→ 本次 National 表的 5.056/1.500 是归一化空间，与论文同空间，偏差真实非空间错配。**
- **推翻"之前接近论文是归一化空间假象"猜想**：既然同空间，之前 PROJECT_STATUS.md 那组"接近论文"的数字必来自**旧训练配置/旧 scaler 约定**（RevIN bug 修复前或不同 epochs），非本次干净训练。本次 8/17 是第一次 bug-fix 后严格论文协议真实数字，暴露 2 倍量级真实差距 → 之前"接近"才是假象。
- **Pre-COVID 子集验证**（`scripts/verify_precovid_mse.py`，abs_week<=935 即 2017-2019）：
  - L24 FULL MSE=6.35 / PRE=3.03 (2.09x)；L36 6.73/2.32(2.90x)；L48 8.60/2.62(3.29x)；L60 6.20/3.60(1.72x)。
  - **Pre-COVID 段 MSE 仍 2.3–3.6，比论文 1.542 高 1.5–2.3x** → 推翻"偏差主要来自 COVID 分布漂移"干净假说。偏差大头来自**代码/协议层面**（epochs=20 偏少？RevIN/StandardScaler 实现细节？超参未披露 lr/batch/LoRA rank？seed 未知）。
- **结论给导师**：归一化空间下 Pre-COVID 段仍偏高 ~2x，说明复现偏差非单纯数据修订/COVID，需进一步归因训练配置；不能宣称 bit-exact 复现。
