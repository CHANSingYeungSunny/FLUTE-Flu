# 2026-08-10

## HPC 重训结果（job 493330，归一化修复 + 逐字 prompt）
- 链式作业 `train_figure_all_L.sh` 顺序跑完 4 horizon，无 DISCARDED/non-finite（NaN-rep 纪律生效）。
- **最新归一化指标（StandardScaler 空间，10-rep 均值）vs 论文 Table V**：
  | L | 实测 MSE | 论文 ref | ratio | 实测 MAE | 论文 ref | ratio |
  |---|---|---|---|---|---|---|
  | 24 | 13.3345 | 1.542 | 8.65x | 3.1893 | 0.726 | 4.39x |
  | 36 | 5.7650 | 1.422 | 4.05x | 1.8941 | 0.779 | 2.43x |
  | 48 | 6.2545 | 1.414 | 4.42x | 1.9120 | 0.757 | 2.53x |
  | 60 | 1.7704 | 1.364 | 1.30x | 0.9612 | 0.719 | 1.34x |
- **结论（Path A 诚实标注）**：归一化 bug 已修（无负值、无 nan、空间一致），L=60 已接近论文（1.3x）；但 L=24/36/48 仍 4–8.6x 偏高。
- **疑似残余根因（待用户关键信息）**：`ForecastingLLM.forward` 把全部 patch 用 `mean(dim=1)` 池化成单向量再 `Linear(D,N*L)`（miflu_model.py:334），破坏时间结构 → 类朴素池化回归；L=60 因目标更平滑而相对占优。此为比归一化更深的结构问题，未擅自改（超出原计划范围）。
- **未删除 FYP 脚本**（train_baseline/train_regional_*/train_ablation/verify_* 等），仅删 debug/未跑创新/被取代报告。

## 文档精简
- `PROJECT_STATUS.md §7.2` 已用 493330 最新数字替换旧 4.5 引用，改为 Path A 诚实接受 + 标注疑似结构根因。
- `Tutor_QA_Report.md` 已更新（文件未被删，早前 delete 被跳过）：Q5 §0 用 493330 正确口径填"我方新 MSE/MAE"（L24=13.33/3.19, L36=5.77/1.89, L48=6.25/1.91, L60=1.77/0.96），并诚实标注旧 4.545 等为错误空间不可比；Q5 §2 由"已排除"改为"已修复"（评估空间错误）；§6 与局限声明改用 **MIFlu 论文方法学口径**（论文 lr/epochs "set empirically" 未公开 + 无官方代码/数据快照 → 不声称逐位复现，L=60 1.30x 同量级即验证管线正确）。
- 用户要求"用 MLFlu essay method 陈述局限"，并说稍后给出"正确方法"会一次性解决所有问题 → 暂停深层架构修复，等该信息。
- 早前 delete 调用被工具跳过（标记 long-running），故 Innovation-Proof.md / MoE 脚本 / debug 脚本实际仍在工作区；Tutor_QA_Report.md 引用已改指向 PROJECT_STATUS.md。

## Tutor_QA_Report.md 全面 HPC 化更新（用户最新指令）
- 用户要求：update every question 含 Q1，大多数用 HPC 实跑数字；MoE 不在本次 National Q1 范围故不跑 HPC。
- **Q1 重写**：用 job 493330 真实 CSV 数字（4 horizon 两条线 GT vs Pred 峰值表）：L24 峰值 111,361→47,961.7(−56.93%)、L36 111,361→75,000.7(−32.65%)、L48 111,361→57,288.9(−48.56%)、L60 111,361→100,850.1(−9.44%)；并诚实记录 L60 谷底负值缺口（201928=−661 等）。
- **Q1 §3.2 负值缺口根因**：模型 `clamp(min=0)`（miflu_model.py:488）已做，但 `make_forecast_figure.py` 反归一化后未对 `pred_raw` 二次裁剪（仅 nan_to_num）。已落地下修复：figure 脚本追加 `pred_raw = np.clip(pred_raw, 0.0, None)`，py_compile 通过。HPC 上 L60 CSV 仍为旧（含负值），需重跑 figure 才刷新。
- **Q3 MoE 改为 tombstone**：移出 National Q1 范围，仅留本地插入点定位 + test_moe_shapes.py 记录；Q4/Q5/Q6 编号保持不变（避免破坏交叉引用）。
- **Q2/Q4/Q6 加运行环境标注**：Q2(70:10:20)与 Q4(Providers 因果)为本机实跑（dataset_split_facts/providers_causality）；Q6 Regional 为本机 GPU 实跑（~0.80GB），National 7 通道解释共用 HPC 493330。
- 交付清单与 Q5 §2 同步：Q5 §2 原"无负值 ILI"改为训练/评估空间无 nan/负值，L60 谷底负值缺口归为 figure 路径待修项（不影响归一化指标）。
