---
name: MLFlu文档精简与文件清理
overview: 精简 MIFlu 复现障碍分析.md（Q1/Q2/Q6 大白话化、Q5 用 L52 新协议结果更新），删除过时/错误交叉参考文件，并更新 memory。训练 job 504263 由我每2分钟只读轮询直至 best_miflu_L52.pth 生成。
todos:
  - id: delete-stale-files
    content: 删除 6 个过时/错误文件（3 txt + json + 2 md）
    status: completed
  - id: poll-training
    content: 每2分钟只读轮询 job 504263 直至 best_miflu_L52.pth 生成
    status: completed
  - id: rewrite-doc
    content: 用大白话精简重写 MIFlu 复现障碍分析.md（Q1/Q2/Q5/Q6）
    status: completed
    dependencies:
      - delete-stale-files
  - id: update-memory
    content: 更新 memory 的删除清单与交叉参考约定
    status: completed
    dependencies:
      - delete-stale-files
      - rewrite-doc
  - id: infer-next
    content: ckpt 生成后提示下一步 sbatch infer_walkforward_L52.sh
    status: completed
    dependencies:
      - poll-training
---

## 用户需求

1. 删除过时/错误交叉参考文件：3 个错误提取 .txt（MIFlu_extracted.txt / MIFlu_layout.txt / page_table2.txt）、miflu_ground_truth.json、REPRODUCTION_PROOF_V2_ZH.md、MLFlu_forecast_figure.md；保留全部 PDF 论文原文与 MIFlu_Complete_Extraction.md（**注：该旧提取文件已于 2026-08-17 被 `MIFlu_paper.md`（PDF 直接 markdown 转换）取代并删除，现唯一交叉参考 = `MIFlu_paper.md`**）。
2. 精简重写 MIFlu 复现障碍分析.md，用零基础学生能懂的大白话、尽量短：

- Q1：只简述有几张图、各图干嘛；并给出结果是否准确（按 Peak Hit / Timing / Peak Intensity / Direction 四个指标 verdict 说明）。
- Q2：缩短，只说用 L=24 的原因（几个字），并解释现在选 pred L=52 而非 L=24 是因为要 follow 医学期刊（如 computational biology）发文的 golden standard（几个字）。
- 为什么选 L=104：4 点理由（年度季节性 / 长期预测业务需求 / 避免过拟合噪声 / 前人范式）各缩短为几个字。
- Q6：只提用到的变量；input/process/output 一句话简述；说明 OT = num_patients/100（论文没明说，黑箱，与论文 table 在 api 对比）；并给出用这 7 个指标做预测的公式。
- Q5：更新所有含论文原始数据的对比，改用新 L52 walk-forward 协议结果；旧 L=24/36/48/60 表标注为"旧协议已被取代"而非删除。

3. 更新 memory 中相关 md 引用（交叉参考改为仅 MIFlu_Complete_Extraction.md，记录删除清单；2026-08-17 起改指向 MIFlu_paper.md）。
4. 训练 job 504263 由我每 2 分钟只读轮询（ssh 查队列/日志/best_miflu_L52.pth），直至 checkpoint 生成即成功。

## 技术栈

- 文档为 Markdown，无代码改动；删除为文件系统操作（PowerShell Remove-Item）；轮询为只读 SSH 命令。
- HPC 训练已在跑（job 504263），无需重提。

## 实施方法

- 删除操作：用绝对路径 Remove-Item，仅删用户确认清单内文件，不碰 PDF 与 MIFlu_Complete_Extraction.md。
- 文档重写：在原文件上整篇重写，保持 Q1–Q6 结构但大幅精简；Q5 旧表保留并加"旧协议"标注，新 L52 结果待 HPC 出图回填（先写占位说明，待训练+推理完成后本机出图再回填真实数字）。
- 轮询：每 2 分钟一次只读 ssh，检查 job 状态/日志末尾/checkpoint 是否生成；发现 NaN/OOM/报错立即报告，ckpt 生成即停止轮询并提示下一步 sbatch infer_walkforward_L52.sh。

## 执行注意

- 删除前不做破坏性操作之外的改动；轮询仅只读，不执行任何修改类命令。
- Q5 新协议数字在 HPC 推理完成、本机出图后回填，当前先留"待回填"标记，避免编造。
- memory 更新与文档删除同步，确保后续对话引用一致。

## 目录结构

```
MLFlu/
├── MIFlu 复现障碍分析.md          # [MODIFY] 整篇精简重写，大白话，Q1/Q2/Q5/Q6 按要求缩短，Q5 旧表加标注
├── MIFlu_paper.md                 # [KEEP] 唯一交叉参考（PDF 直接转换），取代已删的 MIFlu_Complete_Extraction.md（该旧文件已于 2026-08-17 删除，不再引用）
├── miflu_ground_truth.json        # [DELETE] 错误真值基准
├── REPRODUCTION_PROOF_V2_ZH.md    # [DELETE] 旧复现证明，被新协议推翻
├── MLFlu_forecast_figure.md       # [DELETE] 基于退役脚本，不准确
├── reference_papers/
│   ├── MIFlu_extracted.txt        # [DELETE] 错误提取
│   ├── MIFlu_layout.txt           # [DELETE] 错误提取
│   └── page_table2.txt            # [DELETE] 错误提取
└── .codebuddy/memory/
    ├── 2026-08-14.md              # [MODIFY] 追加删除清单与交叉参考变更
    └── MEMORY.md                  # [MODIFY] 更新长期交叉参考约定
```

## Agent Extensions

### SubAgent

- **code-explorer**
- Purpose: 在重写文档前，跨文件确认 Q5 旧表引用、Q6 变量定义、OT=num_patients/100 实现位置等，避免重写遗漏或写错。
- Expected outcome: 定位 MIFlu 复现障碍分析.md 内所有需更新的段落与代码侧依据，保证精简后内容准确。